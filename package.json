{
  "name": "json-map-ts",
  "version": "0.0.4",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "type": "module",
  "description": "Parse/stringify JSON and provide source-map for JSON-pointers to all nodes",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/loggerhead/json-map-ts.git"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest --env=node --colors --coverage test",
    "prepare": "npm run build",
    "prepublishOnly": "npm run test"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/json-pointer": "^1.0.31",
    "jest": "^27.5.0",
    "json-pointer": "^0.6.1",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}
